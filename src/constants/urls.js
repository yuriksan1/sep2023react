const jsonBaseUrl = 'https://jsonplaceholder.typicode.com'
const spaseXBaseUrl = 'https://api.spacexdata.com/v3'

const posts = '/posts'
const launches = '/launches'

const urls = {
    posts,
    launches
}

export{
    jsonBaseUrl,
    spaseXBaseUrl,
    urls
}